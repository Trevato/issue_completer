[
    {
        "name": "take_action",
        "description": "Supply a prompt with what action you would like to take. Based on the prompt a semantic search algorithm will find the proper function to invoke",
        "parameters": {
            "type": "object",
            "properties": {
                "prompt": {
                    "type": "string",
                    "description": "A description of the action to take"
                }
            },
            "required": ["prompt"]
        }
    }
]
